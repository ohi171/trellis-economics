# Trellis Economics website

Static, responsive website for Trellis Economics, an independent economic research, evaluation, and data advisory.

## Pages

- Home
- Services
- Expertise
- Selected Work
- Insights
- About
- Contact

## Preview locally

From the repository directory, run:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Publish with GitHub Pages

1. Open the repository on GitHub.
2. Select **Settings** and then **Pages**.
3. Under **Build and deployment**, select **Deploy from a branch**.
4. Choose the default branch and the `/ (root)` directory.
5. Save and wait for GitHub to publish the site.

For a repository named `trellis-economics` under the account `ohi171`, the expected address is:

`https://ohi171.github.io/trellis-economics/`

## Contact configuration

The initial public contact is `mdohiul.islam@wmich.edu`. Search for that address in `contact.html`, page footers, and `assets/main.js` to replace it consistently.

## Notes

- The contact form opens the visitor's email application and does not store submissions.
- No external fonts, trackers, analytics, or form services are loaded.
- The site is designed to work directly from a GitHub Pages project repository.

